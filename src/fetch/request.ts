import {req, OK, SIZE} from './'

// export const clean: any  = ({data}) => {
//   return data && data.code === OK ? (data.data ? data.data : true) : Promise.reject(data.msg || '')
// // }
// export const special = ({ data } )  => {
//   return data.data
// }